version: '3.8'

services:
  medbot-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET_KEY=super-secret-key
      - REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
